


module.exports = target_handler = async (sock, sev, db, google, msg, target_config, config) => {
    try {
        let {key, messageTimestamp, pushName, message } = msg;
        




    } catch (error) {
        throw error
    }
    

}